{
  "name": "morganinc",
  "version": "0.1.0",
  "private": true,
  "description": "Portfolio website for Morgan Sinclair — morganinc.cc",
  "scripts": {
    "content": "node scripts/build-content.mjs",
    "predev": "npm run content",
    "dev": "wrangler dev",
    "predeploy": "npm run content",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
